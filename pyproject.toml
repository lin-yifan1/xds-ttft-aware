[project]
name = "xds-latency-anomaly"
version = "0.1.0"
description = "MaaS monitor API CLI for cluster latency anomaly detection."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask",
    "numpy",
    "pandas",
    "plotly",
]

[tool.uv]
package = false
